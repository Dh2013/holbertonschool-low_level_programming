#include "main.h"
#include <stdio.h>

/**
 * main - check the code for
 *
 * Return: Always 0.
 */

char *leet(char *a);
{
	char p[] = {'a','e','o','t','l'}
	char c[] = {'A','E','O','T','L'}
	char m[] = { '4','3','0','7','1'}
	int i =0;
for (i =0 ; i < 5; i++)
{
	 if (*a == p[i] || *a ==c[i])
	 { 
		 *a = n[i] +'0';

}	
a++
} 
return 0;
